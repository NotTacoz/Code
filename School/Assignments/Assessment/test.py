import numpy as np
import random
# this is a test file to test all the neural networks maths


## dot product + bias
input_vector = [1.72,1.23]
weights_1 = [1.26, 0]

dot_product = np.dot(input_vector, weights_1)
