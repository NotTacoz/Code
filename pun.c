/*Name: pun.c*/
/*Purpose: Prints a bad pun.*/
/*Author: K. N. King*/
#include <stdio.h>

int main(void)
{
  int height = 8; // defining an integer variable
  float profit; // a float type variable
  int length, width, volume; // multiple variables defining

  length = 2;
  width = 10;

  volume = height * length * width;
  
  // decleration + statement goes in here
  printf("To C, or not to C: that is the question. Also the volume is %d\n", volume);
  return 0;
} // this is the second type of comment !

